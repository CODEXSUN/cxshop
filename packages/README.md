# CXShop Packages

Packages contain stable technical contracts and business-neutral controls.

Planned packages:

- `framework` contains runtime, transport, configuration, database, queue, and observability contracts.
- `contracts` contains approved public integration and client contracts.
- `ui` contains design-system controls without marketplace business rules.

Do not move product fields, workflows, permissions, forms, or repositories into a shared package.
